---
import { getCollection } from "astro:content";
import BaseLayout from "@layouts/BaseLayout.astro"
const allPosts = await getCollection("posts");
---

<BaseLayout>
 {allPosts.map((post) =>(<p><a href={`/posts/${post.slug}/`}>{post.data.title}</a></p>))}
</BaseLayout>